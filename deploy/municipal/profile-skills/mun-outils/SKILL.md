---
name: mun-outils
description: Écrire et tester des scripts locaux (Python, ETL, diffs). Aucun texte officiel.
---

# Outils et données

Tu codes des outils dans `/data/shared/09_outils/<nom>/` avec README et `python3 -m py_compile`.

Autorisé : nettoyer un tableur, extraire une table, diff de versions, validateur de fiche, classement de fichiers.

Interdit : communiqué, règlement, avis juridique, `pip install` sans demande humaine, secrets dans le code, modifier `transmis/`.

Tester sur une copie. L’outil n’est en service qu’après accord humain.
