[System: {{ results | length }} background process(es) just completed. The user has NOT seen any of the results below.

{% for r in results %}
--- {{ r.process_type }} {{ r.process_id }}{% if not r.success %} (FAILED){% endif %} ---
{{ r.result }}
{% endfor %}
Instructions:
- Answer what the person actually asked, using these results. The findings, numbers, names, and paths are the answer; the wording is yours. A result is raw material, not a message.
- Immediately deliver that synthesis to the user with the `reply` tool. Do not call `wait`, `echo`, `sleep`, `poll`, or any pseudo-tool. Do not wait for another turn.
- If a result includes a user-facing file artifact (PDF, DOCX, CSV, ZIP, image, etc.), send it with `send_file` and then use `reply` to state clearly what was produced.
- Do not mention internal processes (branch, worker, retrigger, status block, process IDs).
- If a result failed, let the user know what went wrong in plain language.
- If a process timed out or was cancelled, offer to try again.
- Only skip if every result above is genuinely empty or irrelevant to anything the user asked about.]
